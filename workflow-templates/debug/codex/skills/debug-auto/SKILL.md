---
name: debug-auto
description: 当用户显式要求使用 "debug auto"、"debug-auto"、"自动化 debug"、"全自动排查前端 bug" 时使用。适用于可以由 agent 用 Playwright 或等价浏览器自动化能力自行启动项目、自动操作页面、监听 console/pageerror/network 日志并基于证据分析问题的场景。
---

# Debug Auto

## 触发条件

只在用户明确要求时启用，例如：

- `使用 debug auto`
- `请用 debug-auto 自动排查这个问题`
- `走自动化 debug，你直接操作页面并看日志`

如果用户没有显式要求，不要默认启用。

## 目标

该 skill 用于让 agent 尽量独立完成整轮前端排障：

1. 启动或连接本地项目
2. 使用 Playwright 或等价浏览器自动化能力复现问题
3. 自动监听 console、page error、request failed、关键响应
4. 先基于运行证据定位，再进入代码修复
5. 修复后再次自动验证

## 核心原则

### 自动化优先

进入 `debug-auto` 后，默认目标是让 agent 自己完成启动、操作、取证和复测。

除非遇到明显阻塞，例如：

- 必须真人扫码、验证码或硬件操作
- 复现依赖外部账号权限且当前环境不可用
- 问题只在用户私有数据或本机特定环境中出现

否则不要过早把操作步骤转交给用户。

### 证据优先

自动化执行拿到的运行证据是一手依据，至少包括：

- `console` 输出
- `pageerror`
- 失败请求与关键响应
- 复现步骤中的页面状态变化

在还没拿到这些证据前，不要仅凭代码直觉下根因结论。

### 无法自动化时及时降级

如果问题无法在当前环境被 agent 稳定自动复现，不要勉强继续“半自动”排查。

应明确说明阻塞原因，并建议切换到更合适的模式：

- 用户可以自己操作页面时，切到 `debug-steps`
- 只能通过代码打点取证时，切到 `debug-manual`

## 标准流程

### 1. 确认启动方式并拉起项目

先查看项目已有的启动脚本、开发说明或测试命令，确定最小可运行路径。

优先：

- 使用已有 `dev` / `start` / `preview` / `test:e2e` 脚本
- 记录实际访问地址、账号前提和必要环境变量

如果项目已在运行，可直接复用，不要重复启动多个实例。

### 2. 使用 Playwright 监听运行时证据

打开页面前就挂上监听，不要等到失败后再补采：

- `console`
- `pageerror`
- `requestfailed`
- 关键 `response`

必要时补充：

- 截图
- DOM 快照
- URL / local storage / cookie / 关键状态值

## 3. 自动复现问题

根据用户描述自行操作页面，尽量把复现路径结构化记录出来：

- 访问哪个页面
- 点击或输入了什么
- 在哪一步出现异常
- 异常发生前后收到了哪些日志或请求结果

如果首次复现失败，应先调整自动化步骤或补充观测点，而不是立刻转去改代码。

### 4. 先总结证据，再读代码

在查看实现前，先把自动化运行中已经确认的事实讲清楚，例如：

- 哪一步稳定触发问题
- 哪条报错最关键
- 哪个请求失败或返回异常
- 哪个组件状态和预期不一致

然后再根据这些事实去定位代码路径与修复点。

### 5. 修复后自动回归

完成修改后，必须重新运行自动化步骤验证：

- 原问题不再出现
- 关键日志恢复正常
- 没有引入新的明显报错

如果修复后仍无法用自动化复测通过，不要直接把问题标记为解决。

## 行为约束

1. 不要启动 `debug-steps` 用的本地日志服务器。
2. 不要把“我看代码觉得这里像有问题”当成首轮定位结果。
3. 不要在没有自动化证据的情况下直接开始正式修复。
4. 如果自动化路径被真实阻塞，要明确停止原因，并建议切换 skill，而不是继续模糊推进。

## 推荐输出骨架

在 `debug-auto` 中，尽量按这个顺序对用户汇报：

1. `我已经拉起了项目并开始自动复现`
2. `自动化运行里确认到的现象`
3. `这些现象对应的代码路径`
4. `我准备修改或已经修改的点`
5. `自动回归验证结果`
