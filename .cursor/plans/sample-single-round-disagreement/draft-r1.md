# 内部服务调用网关分析

## 任务理解

需要分析是否应在所有内部服务之间统一引入同步调用网关。

## 目标

- 评估统一入口是否能简化治理
- 识别对现有服务通信模式的影响

## 范围

- 只分析内部服务间调用
- 不覆盖外部 API 网关

## 假设

- 大多数内部调用都适合同步 HTTP 模式

## 风险

- 可能放大链路延迟
- 可能把异步场景错误收敛为同步场景

## 建议方案

统一要求所有内部服务调用都先接入同步调用网关，再由网关路由到目标服务。

## 验证方式

- 评估接入前后的链路延迟

## 开放问题

- 是否要保留事件驱动场景
